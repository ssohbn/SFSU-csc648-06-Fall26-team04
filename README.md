# group project
some info

# setup & install

## deps
sqlite3 and golang are required for this project

for debian based distros just hit this and ur good!
``sudo apt install sqlite3 golang``

## running the project
run ``go run main.go`` and follow the instructions in your terminal!
